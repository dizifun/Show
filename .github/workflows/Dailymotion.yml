name: Update M3U Playlist

on:
  schedule:
    # Her 4 saatte bir çalıştır
    - cron: '0 */4 * * *'
  workflow_dispatch: # Manuel tetikleme

# 👇 BURASI ÇOK ÖNEMLİ: Bota yazma izni veriyoruz
permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Depoyu Çek (Checkout)
      uses: actions/checkout@v3
      with:
        # persist-credentials: true varsayılan olarak zaten true'dur, 
        # bu sayede sonraki adımlarda git push yapabiliriz.
        persist-credentials: true

    - name: Python Kurulumu
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Kütüphaneleri Yükle
      run: |
        python -m pip install --upgrade pip
        pip install requests

    - name: M3U Oluşturucu Scripti Çalıştır
      run: python generator.py

    - name: Değişiklikleri Commit ve Push Yap
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'
        
        git add playlist.m3u
        
        # Eğer değişiklik varsa commit yap, yoksa "Değişiklik yok" de ve devam et
        if git diff --staged --quiet; then
          echo "📭 Değişiklik yok, commit yapılmadı."
        else
          git commit -m "🚀 Playlist güncellendi: $(date)"
          git push
        fi
