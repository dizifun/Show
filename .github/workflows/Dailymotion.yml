name: Update M3U Playlist

on:
  schedule:
    # Her 4 saatte bir çalıştır
    - cron: '0 */4 * * *'
  workflow_dispatch: # Manuel tetikleme

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Depoyu Çek (Checkout)
      uses: actions/checkout@v3

    - name: Python Kurulumu
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Kütüphaneleri Yükle
      run: |
        python -m pip install --upgrade pip
        pip install requests

    - name: M3U Oluşturucu Scripti Çalıştır
      run: python generator.py

    - name: Değişiklikleri Commit ve Push Yap
      run: |
        git config --global user.name 'GitHub Actions Bot'
        git config --global user.email 'actions@github.com'
        git add playlist.m3u
        # Eğer değişiklik yoksa hata verme
        git diff --quiet && git diff --staged --quiet || git commit -m "🚀 Playlist güncellendi: $(date)"
        git push